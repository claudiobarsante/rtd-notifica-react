import styled from 'styled-components';

export const Container = styled.button`
  background: transparent;
  border: 0;
  margin-left: 3rem;

  svg {
    color: var(--purple);

    height: 4.5rem;
    width: 4.5rem;
  }
`;
